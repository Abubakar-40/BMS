# BMS
A Django + DRF training project covering banks, branches, accounts, and transactions. Builds from core Django (models, admin, ORM) through DRF (serializers, permissions,     ▎ filtering, pagination) to production features like auth, caching, Celery background tasks, and analytics reporting APIs.
